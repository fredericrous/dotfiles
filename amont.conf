# Checks this repository declares for itself (see amont's docs/custom-checks.md).
#
# stage       name          scope  severity  command
pre-commit    private-refs  *      block     scripts/check-private-refs.sh
