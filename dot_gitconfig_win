[user]
    name = Zougi
    email = ***REMOVED***
[alias]
    rewrite-author = !~/.git-rewrite-author.sh
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen%ad (%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=local
    st = status
    co = checkout
    ca = commit --amend
    br = branch
#    p4 = !'C:\\Program Files\\git\\bin\\git-p4.py'
[branch]
    autosetupmerge = true
[color] 
    ui = auto 
[color "branch"] 
    current = yellow bold
    local = white
    remote = green 
[color "diff"] 
    meta = yellow bold 
    frag = magenta bold 
    old = red bold 
    new = green bold 
    commit = yellow bold 
[color "status"] 
    added = cyan 
    changed = green 
    untracked = red bold 
[core]
    eol = lf
    autocrlf = input
    safecrlf = false
    editor = "'C:/Program Files/Sublime Text 3/subl.exe' -w"
[credential]
    helper = cache
[diff]
    tool = p4merge
[difftool]
    prompt = false
[difftool "p4merge"]
    cmd = p4merge "$LOCAL" "$REMOTE"
    keepTemporaries = false
    trustExitCode = false
    keepBackup = false
[filter "lfs"]
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
[http]
    sslVerify = false
    postBuffer = 157286400
#[hub]
#    host = enterprise.corp
[merge]
    keepBackup = false
    tool = p4merge
[mergetool]
    prompt = false
[mergetool "p4merge"]
    cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
    keepTemporaries = false
    trustExitCode = false
    keepBackup = false
[pull]
    rebase = true
[rebase]
    autoStash = true
[push]
   default = current
#[includeIf "gitdir:~/Developer/Company/**"]
#    path = ~/.gitconfig_company